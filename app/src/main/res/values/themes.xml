<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProjetoAuto" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_dark</item>
        <item name="colorPrimaryVariant">@color/blue_dark</item>
        <item name="colorOnPrimary">@color/blue_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/blue_dark</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Stylo barra superior -->
    <style name="Theme.ProjetoAuto1" parent="Theme.ProjetoAuto">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Stylo barra superior -->
    <style name="Theme.ProjetoAuto2" parent="Theme.ProjetoAuto">
        <item name="android:statusBarColor">@color/blue_dark</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Style da progressbar -->
    <style name="ProgressBar" parent="Theme.ProjetoAuto">
        <item name="colorAccent">@color/blue_dark</item>
    </style>


    <style name="CustomStyleTabLayout" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:textSize">14sp</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- Style da barra de menu -->
    <style name="ThemeBottomNavigationOn">
        <item name="android:textSize">14sp</item>
    </style>

    //Style da bottomsheet

    <style name="BottomSheetDialog" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyleWrapper</item>
    </style>

    <style name="BottomSheetStyleWrapper" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    //Style calendario

    <style name="CalendarioText" parent="@style/TextAppearance.AppCompat.Small">
        <item name="android:textSize">18dp</item>
    </style>

    <style name="AppTheme.PopupOverLay" parent="ThemeOverlay.AppCompat.Light" />


</resources>